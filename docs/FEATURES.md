# Features

Shipped, working capabilities. Move items here from `BACKLOG.md` once they ship and are verified.

## Dev workflow skill suite

The `.claude/skills/dev-*` orchestration pipeline: `dev-orchestrator` (entry point + mode gate), `dev-investigate`, `dev-brainstorm`, `dev-writing-plan`, `dev-build`, `dev-session-wrap`, plus the orchestrator's `references/` (communication, mental models, priming anti-patterns, review gates).
