# Invariants

Non-obvious constraints and load-bearing facts that aren't discoverable from a quick read. The dev-* skills read this file during brainstorm (always) before design.

_No project-specific invariants yet._
